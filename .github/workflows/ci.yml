name: CI

on:
  push:
    branches:
      - main-continued

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Run a simple check
        run: echo "CI is working on branch ${{ github.ref }}"
